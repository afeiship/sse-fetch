import sseFetch from '../src';

describe('api.basic', () => {
  test('Normal single value case', () => {
    // sseFetch('/api/stream', {
    //   onMessage: (event) => {
    //     // expect(event.data).toBe('Hello, World!');
    //   },
    // });
  });
});
